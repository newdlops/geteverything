# 2026-09-18 수량·단가·기준환율 운영 반영

## 반영 내용

- `deals.0010_deal_measurements`를 18:54 KST에 적용했다. 수집 가격 원문, 별도 수치 작업 테이블, 입력 변경 트리거가 추가됐다.
- 웹 API와 Django 어드민에 중량·용량·개수·단가·배송비·비교용 원화 환산을 추가했다. 어드민 경로는 `/admin/deals/dealmeasurements/`다.
- 수치 계산은 서버 내부 규칙과 Decimal을 사용한다. 외부 통신은 ECB 공식 환율 문서 조회이며 6시간마다 최신 일별 자료를 확인한다. 로컬 LLM은 기존처럼 검토 제안만 남긴다.
- `rules-2`의 단어 보완과 부분 문자열 충돌 수정을 적용했다. 수동 분류는 유지한다.
- 수집 가격 원문을 보존하도록 크롤러를 19:08 KST에 한 번 교체했다. 이후 `measurements-2` 용기 용량 보완은 웹과 계산 워커에만 적용했다.

## 최종 관측 — 2026-09-18 21:11 KST

| 항목 | 결과 |
| --- | --- |
| 전체 게시물 / 수치 결과 | 43,402 / 43,402 |
| 단가 계산 완료 | 10,035 |
| 확인 가능한 일부 정보 제공 | 28,194 |
| 수량 관계 등 불명확하여 계산 검토 | 5,173 |
| 수치 계산 대기 / 오류 | 0 / 0 |
| 기존 데이터 백필 | 43,349건 스캔, `complete` |
| 이전 자동 분류 규칙 재등록 | 43,192건; `rules-1` 결과 없음 |
| 수집 가격 원문이 저장된 게시물 | 462 |
| 최신 저장 환율 기준일 | ECB 2026-09-17 |

이 수치는 관측 시점의 스냅샷이다. 백필 스캔 이후 신규 수집분도 처리됐으므로 전체 결과 수와 스캔 수는 다르다. `partial`에는 가격이나 수량이 없는 상품, 단위 가격이 적합하지 않은 상품도 포함되며 계산 오류를 뜻하지 않는다.

19:17 KST에 보관한 이전 규칙의 검토 대상 500건을 같은 ID로 다시 확인했다. 60건은 `rules-2`에서 자동 분류됐고 440건은 검토 상태를 유지했다. 이는 검토 상태 변화이며 독립적으로 검증한 분류 정확도가 아니다.

## 실제 데이터 확인

- 뽐뿌 `자연산 손질 통오징어 320g x 3팩 (6마리) (14,070원/무료)`는 총 960g, 100g당 1,465.63원, 팩당 4,690원으로 계산됐다. 요청 후 약 0.68초에 처리됐다.
- FMKOREA `농심 신라면 건면 97g, 20개`는 총 1,940g, 15,300원 기준 100g당 788.66원, 개당 765원으로 계산됐다.
- ARCA `팔도 밥알없는 비락식혜, 175ml, 30개`는 총 5,250ml, 8,410원 기준 100ml당 160.19원으로 계산됐다.
- 용기 상품 92건을 입력 revision 조건으로 재등록했다. 게시물 132484의 텀블러는 표시 용량 600ml와 개당 7,250원을 제공하고, 132490의 보틀은 표시 용량 710ml를 제공한다. 두 상품 모두 내용물의 100ml당 단가를 만들지 않음을 DB와 운영 API에서 확인했다.
- 해외 상품 131394는 원가 USD 120과 공식 기준일을 포함한 원화 환산 결과를 반환한다. 과거 정수 가격의 소수점 원문이 없다는 주의사항도 유지된다.
- 5개 커뮤니티의 수집 갱신을 확인했다. 크롤러와 계산 워커는 실행 중이며 OOM 기록과 재시작 횟수는 각각 0이었다. 기존 FlareSolverr의 재시작 횟수 1은 이번 배포와 무관하다.
- 썸네일 워커는 실행 중이며 재시작 횟수 0, 썸네일 요청 대기 0이었다.
- 계산 워커 제한은 0.15 CPU·192 MiB를 유지한다. 마지막 관측의 호스트 CPU는 79.3%, 가용 메모리는 약 8.7 GiB였고, 로컬 LLM은 부하 보호 정책에 따라 정지 상태였다.

## 검증

- 분류·수치 테스트 43개, 크롤러 관련 테스트 18개, 썸네일 테스트 42개 통과. 최종 웹 이미지에서도 분류·수치 및 썸네일 테스트를 실행했다.
- 격리한 PostgreSQL 스키마에서 트리거의 입력 변경, revision, 원문 소수점, 트랜잭션 롤백을 확인했다. 운영 상품 행은 이 테스트로 변경하지 않았다.
- migration drift 검사와 diff 공백 검사를 통과했다.
- 로컬의 동일 어드민 코드를 실제 Chromium에서 1440×900, 768×1024, 390×844로 확인했다. 목록·상세·검색·필터·빈 결과·대기·오류·분류 상세 연결·모바일 가로 스크롤을 검증했고 자바스크립트 오류가 없었다. 화면 검증은 테스트 데이터로 했으며, 운영 로그인 후 화면을 직접 확인한 것은 아니다.
- 운영 API 목록, 해외 상품 상세, 용기 상품 상세와 정적 CSS가 HTTP 200을 반환했다. 어드민 미인증 접근은 로그인 화면으로 연결됐다.

## 배포 및 복구 기록

- 웹 최종 이미지: `my-django-app:measurements-20260918-r2` (`4bcc48680122`). 직전 이미지: `my-django-app:measurements-20260918`.
- 계산 워커 최종 이미지: `1d23fc4a0cac`; 직전 워커 설정은 `/root/category-worker-backups/1789731315476396889/runtime.json`에 있다.
- 크롤러 이미지: `crawler-image:measurements-20260918` (`4d793e1c890d`). 19:08 KST 이후 변경하지 않았다.
- 웹 기록: `/root/measurements-web-20260918`, `/root/measurements-web-20260918-r2`.
- 크롤러·워커 기록: `/root/measurements-crawler-20260918`, `/root/measurements-worker-20260918-r2`.
- 이전 크롤러 컨테이너 `crawlers-bot-before-measurements-20260918`는 정지 상태이며 자동 재시작 정책은 `no`다. 복구할 때도 두 크롤러를 동시에 실행하지 않는다.

서버 백업에는 비밀 설정이 포함될 수 있으므로 root 전용으로 유지한다. 되돌릴 때에는 우선 코드와 컨테이너 이미지를 복구한다. 추가 DB 스키마는 이전 코드와 호환되므로 데이터를 삭제하는 역방향 migration을 실행할 필요가 없다.

혼합 구성·선택형 수량·불분명한 가격은 계산을 보류한다. 이미 정수로 저장되어 유실된 외화 소수점은 원문을 다시 수집하기 전까지 복원할 수 없다. `measurements-1`의 영향 없는 기존 결과는 유지하며, 용기 용량 보완 대상과 신규 요청부터 `measurements-2`를 사용한다.
