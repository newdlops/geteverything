# 2026-09-22 운영 상품 모델 백업

- 운영 실행: `72df724238fe4453ac92`, 상품 프롬프트 `product-sft-6`.
- 학습 388개 제목, 1,800 step의 최종 체크포인트를 채택했다. 원본 학습 실행은 `37f8e4b7f3556e079055`이며 선택 과정에서 추가 학습은 하지 않았다.
- 서버 검증: 운영 조건 원본 **73/100**, 동일 프롬프트 원본 **68/100**, 최종 학습 모델 **100/100**. 기존 정답 퇴행, 오병합, 동일 상품의 잘못된 분리는 모두 0건이다.
- 검증 100개 제목에는 쇼핑몰·가격 표기 변형이 포함되며, 정규화 후 25개 입력이다. 동일 비교군의 같은 입력만 재사용하고 원문별 상품 연결을 따로 채점했다. 검증 결과의 범위는 이 데이터셋이다.
- 기존 두 비교군은 기반 모델·추론 이미지·프롬프트·평가 코드·검증 데이터의 일치를 확인한 뒤 재사용했다. 최종 후보는 서버에서 새로 추론했다. 원본 비교 결과와 체크섬은 `meta/server-control-*`에 보존했다.
- 서버 후보 응답 p95는 53.022초, 운영 추론 CPU 한도는 0.2코어, 메모리 한도는 3GiB다.
- 운영 반영 후 실제 상품 추출에서 새 어댑터 해시와 AAA 건전지 인식 성공을 확인했다. DB 쓰기 없는 확인 호출이며, 별도로 크롤링 갱신과 상품 작업 진행을 확인했다.
- 모델 시작 확인과 어댑터 제어 요청 대기 보완은 `590cc65`에 포함된다. 관련 테스트는 로컬·서버에서 각각 8개 통과했다. 자동 학습 타이머를 재개했다.

`adapter.gguf`와 `best/`가 채택한 가중치다. `checkpoint/`에는 동일 학습의 원본 재개용 가중치와 optimizer가 있다. Git LFS 파일을 내려받아 복구하며, 평가·운영 확인 원문은 `meta/server-*`, 학습 데이터와 코드 스냅샷은 `meta/`에 있다.

기반 모델은 `Qwen/Qwen3.5-2B`, revision `15852e8c16360a2fea060d615a32b45270f8a8fc`이다. 운영 기반 파일은 `Qwen3.5-2B-Q4_K_M.gguf`, SHA-256 `aaf42c8b7c3cab2bf3d69c355048d4a0ee9973d48f16c731c0520ee914699223`이다. 기반 모델과 재생성 가능한 활성화 캐시는 이 백업에 포함하지 않는다.

어댑터 GGUF SHA-256: `000ab490c4eceb5ad6bc990ea9b25e84fa9025ad1029565f85515a740c5ac41e`. 변환 파일의 표시 이름이 `Checkpoint`에서 `Best`로 달라지는 경우의 비교 기록은 `meta/*conversion-verification.json`에 있다. 14개 텐서의 바이트는 동일하다.
